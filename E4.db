SELECT * FROM clientes WHERE nome LIKE 'A%';

SELECT c.cliente_id, c.nome AS nome_cliente, SUM(p.valor) AS valor_total_pedidos
from clientes c JOIN pedidos p ON c.cliente_id = p.cliente_id
GROUP BY c.cliente_id, c.nome;

SELECT cidade, AVG(idade) as media_idade from clientes 
GROUP BY cidade;